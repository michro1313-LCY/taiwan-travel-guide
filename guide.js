(function(){const id=new URLSearchParams(location.search).get('id'),g=window.GUIDES[id],main=document.querySelector('#guideMain');if(!g){document.title='找不到攻略｜Michael 的台灣旅遊攻略';main.innerHTML='<section class="not-found"><p class="eyebrow">GUIDE NOT FOUND</p><h1>找不到這篇攻略</h1><a class="primary-button" href="index.html#guides">回到全部攻略</a></section>';return}document.title=`${g.title}｜Michael 的台灣旅遊攻略`;const names=g.stops.map(s=>s[0]);const route=`https://www.google.com/maps/dir/${names.map(encodeURIComponent).join('/')}`;const poster=g.image?`<figure class="detail-poster"><img src="${g.image}" alt="${g.title}海報"></figure>`:'';const stops=g.stops.map((s,i)=>`<li class="route-stop"><span class="stop-number">${String(i+1).padStart(2,'0')}</span><div><p class="stop-kicker">第 ${i+1} 站</p><h3>${s[0]}</h3><p>${s[1]}</p><dl><div><dt>移動／抵達</dt><dd>${s[2]}</dd></div><div><dt>建議停留</dt><dd>${s[3]}</dd></div></dl></div><a class="map-pin" target="_blank" rel="noopener" href="https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(s[0])}">開啟地圖 ↗</a></li>`).join('');const sources=g.sources.map((s,i)=>`<li><span>${String(i+1).padStart(2,'0')}</span><a target="_blank" rel="noopener" href="${s[1]}">${s[0]} ↗</a></li>`).join('');main.innerHTML=`<section class="detail-hero"><div><a class="back-link" href="index.html#guides">← 回到全部攻略</a><p class="eyebrow">${g.type} · VERIFIED ITINERARY</p><h1>${g.title}</h1><p class="detail-lead">${g.intro}</p><div class="detail-facts"><span>📍 ${g.location}</span><span>⏱ ${g.duration}</span><span>✓ 查證日期 2026-09-06</span></div></div>${poster}</section><section class="detail-layout"><article><section class="transport-panel"><p class="eyebrow">GETTING THERE</p><h2>交通方式</h2><ul>${g.transport.map(x=>`<li>${x}</li>`).join('')}</ul><a class="primary-button" target="_blank" rel="noopener" href="${route}">開啟完整 Google 地圖路線 ↗</a></section><section id="route" class="route-section"><p class="eyebrow">ROUTE ORDER</p><h2>完整行程順序</h2><ol class="route-list">${stops}</ol></section></article><aside class="verification-card"><p class="eyebrow">VERIFICATION</p><h2>查證說明</h2><p>${g.note}</p><p class="small-note">距離與時間標示「約」者，為官方周邊距離、公開路線或地圖步行估算；入口、施工、天候與交通會造成差異。</p><div class="verified-date"><strong>最後查證</strong><span>2026-09-06</span></div></aside></section><section id="sources" class="source-section"><p class="eyebrow">SOURCES</p><h2>查證來源</h2><ol>${sources}</ol></section>`})();
